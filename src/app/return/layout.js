export const metadata = {
  title: "Return Policy | Ijack Paper Products",
  description: "Return policy for Ijack Paper Products. We do not offer returns on any product.",
};

export default function ReturnLayout({ children }) {
  return children;
}
